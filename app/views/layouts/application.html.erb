<!DOCTYPE html>
<html lang="ko" class="h-full">
  <head>
    <title>Mariem</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="utf-8">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style>
      :root { --sidebar-width: 260px; }
      .sidebar { width: var(--sidebar-width); }
      .main-content { margin-left: var(--sidebar-width); }
      @media (max-width: 768px) {
        .sidebar { transform: translateX(-100%); position: fixed; }
        .sidebar.open { transform: translateX(0); }
        .main-content { margin-left: 0; }
      }
    </style>
  </head>

  <body class="h-full bg-zinc-50">
    <% if user_signed_in? %>
      <!-- Sidebar -->
      <aside class="sidebar fixed inset-y-0 left-0 z-50 bg-white border-r border-zinc-200 flex flex-col">
        <!-- Logo -->
        <div class="h-16 flex items-center px-6 border-b border-zinc-100">
          <span class="text-xl font-bold text-zinc-900">Mariem</span>
        </div>

        <!-- Nav -->
        <nav class="flex-1 p-4 overflow-y-auto">
          <div class="menu">
            <%= link_to root_path, class: "menu-item #{current_page?(root_path) ? 'active' : ''}" do %>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/></svg>
              대시보드
            <% end %>
          </div>

          <div class="mt-6">
            <div class="px-3 mb-2 text-xs font-semibold text-zinc-400 uppercase tracking-wider">거래</div>
            <div class="menu">
              <%= link_to transactions_visits_path, class: "menu-item #{request.path.start_with?('/transactions') ? 'active' : ''}" do %>
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"/></svg>
                거래 내역
              <% end %>
            </div>
          </div>

          <div class="mt-6">
            <div class="px-3 mb-2 text-xs font-semibold text-zinc-400 uppercase tracking-wider">고객</div>
            <div class="menu">
              <%= link_to customers_path, class: "menu-item #{request.path.start_with?('/customers') ? 'active' : ''}" do %>
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
                고객 관리
              <% end %>
            </div>
          </div>

          <div class="mt-6">
            <div class="px-3 mb-2 text-xs font-semibold text-zinc-400 uppercase tracking-wider">마스터</div>
            <div class="menu">
              <%= link_to masters_services_path, class: "menu-item #{request.path.include?('/masters/services') ? 'active' : ''}" do %>
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M14.121 14.121L19 19m-7-7l7-7m-7 7l-2.879 2.879M12 12L9.121 9.121m0 5.758a3 3 0 10-4.243 4.243 3 3 0 004.243-4.243z"/></svg>
                서비스
              <% end %>
              <%= link_to masters_products_path, class: "menu-item #{request.path.include?('/masters/products') ? 'active' : ''}" do %>
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"/></svg>
                상품
              <% end %>
              <%= link_to masters_staff_members_path, class: "menu-item #{request.path.include?('/masters/staff') ? 'active' : ''}" do %>
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/></svg>
                직원
              <% end %>
              <%= link_to masters_vendors_path, class: "menu-item #{request.path.include?('/masters/vendors') ? 'active' : ''}" do %>
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/></svg>
                공급업체
              <% end %>
            </div>
          </div>

          <div class="mt-6">
            <div class="px-3 mb-2 text-xs font-semibold text-zinc-400 uppercase tracking-wider">재고</div>
            <div class="menu">
              <%= link_to inventory_purchases_path, class: "menu-item #{request.path.start_with?('/inventory') ? 'active' : ''}" do %>
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M5 8h14M5 8a2 2 0 110-4h14a2 2 0 110 4M5 8v10a2 2 0 002 2h10a2 2 0 002-2V8m-9 4h4"/></svg>
                입고 관리
              <% end %>
            </div>
          </div>
        </nav>

        <!-- User -->
        <div class="p-4 border-t border-zinc-100">
          <div class="flex items-center gap-3">
            <div class="avatar avatar-md"><%= current_user.email[0].upcase %></div>
            <div class="flex-1 min-w-0">
              <p class="text-sm font-medium text-zinc-900 truncate"><%= current_user.email.split('@').first %></p>
              <p class="text-xs text-zinc-500"><%= current_store&.name %></p>
            </div>
            <%= link_to destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-ghost btn-sm" do %>
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/></svg>
            <% end %>
          </div>
        </div>
      </aside>

      <!-- Main -->
      <main class="main-content min-h-screen">
        <!-- Header -->
        <header class="h-16 bg-white border-b border-zinc-200 flex items-center justify-between px-6 sticky top-0 z-40">
          <div class="flex items-center gap-4">
            <button class="md:hidden btn btn-ghost btn-sm" onclick="document.querySelector('.sidebar').classList.toggle('open')">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/></svg>
            </button>
            <h1 class="text-lg font-semibold text-zinc-900"><%= content_for(:page_title) || "" %></h1>
          </div>
          <%= link_to new_transactions_visit_path, class: "btn btn-primary" do %>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/></svg>
            새 거래
          <% end %>
        </header>

        <!-- Flash -->
        <% if notice %>
          <div class="mx-6 mt-4 alert alert-success">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
            <%= notice %>
          </div>
        <% end %>
        <% if alert %>
          <div class="mx-6 mt-4 alert alert-error">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
            <%= alert %>
          </div>
        <% end %>

        <!-- Content -->
        <div class="p-6"><%= yield %></div>
      </main>
    <% else %>
      <%= yield %>
    <% end %>
  </body>
</html>
